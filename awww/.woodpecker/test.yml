when:
  - event: tag

steps:
  test:
    image: rust
    environment:
      CARGO_TERM_COLOR: always
    commands:
      - sudo apt install -y libwayland-dev wayland-protocols
      - cargo test -j1 --workspace --locked
